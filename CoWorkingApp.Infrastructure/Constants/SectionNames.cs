namespace CoWorkingApp.Infrastructure.Constants;

/// <summary>
/// Clase que contiene los nombres de las secciones de configuración.
/// </summary>
public static class SectionNames
{
    /// <summary>
    /// Nombre de la sección de configuración para Swagger.
    /// </summary>
    public const string Swagger = nameof(Swagger);

    /// <summary>
    /// Nombre de la sección de configuración para JWT (Json Web Token).
    /// </summary>
    public const string JsonWebToken = nameof(JsonWebToken);
}
